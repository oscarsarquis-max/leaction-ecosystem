# SEGSENSE_DOM_003 — DemonstrationStory

## Controle

| Campo | Valor |
|---|---|
| Identificador | SEGSENSE_DOM_003 |
| Versão | 1.0 |
| Data | 12/09/2026 |

Agregado editorial local. Não é produto Icatu, adapter, route nem provider contract.

## Ciclo

`workflow_status`: `DRAFT` → `UNDER_REVIEW` → `APPROVED` (ou retorno a novo `DRAFT`).

`publication_status`: `UNPUBLISHED` → `LIVE` ⇄ `PAUSED` → `RETIRED`.

`PUBLISHED`/`LIVE` publica **somente** narrativa na rota SegSense. Não ativa API nem seguradora.

Revisão histórica congelada (`frozen`). Mudança posterior gera nova revisão. Quem submete não aprova. Alegação factual exige fonte `VERIFIED` ou `VERIFIED_LIMITED` de `SEGSENSE_SRC_001`.
