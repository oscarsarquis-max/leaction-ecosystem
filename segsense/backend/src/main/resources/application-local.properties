# Explicit local profile. HTTP loopback for SEGSENSE_PUBLIC_BASE_URL is allowed
# only while this profile is active. Production must not activate `local`.
segsense.demo.protection-journey.enabled=true