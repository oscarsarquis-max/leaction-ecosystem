# QMind — DDL v0

- Status: Aceito (implementação)
- Data: 2026-08-03
- **Freeze de domínio:** git tag [`domain-docs-v0`](https://github.com/oscarsarquis-max/leaction-ecosystem/releases/tag/domain-docs-v0) (pacote documental Aceito)
- Emendas de implementação: `../04_Docs/007_Domain_Docs_Amendment_001.md`
- Motor: PostgreSQL, database **`qmind`**
- Ferramenta: **Alembic** (ver emenda #1 vs ADR-005)

## 1. Objetivo

Materializar o ER lógico e o dicionário Aceitos em `domain-docs-v0` com:

- UUIDs e `timestamptz`
- FKs compostas same-`organization_id`
- CHECKs de estados, aplicabilidade e segregação
- `legal_hold` como flag
- auditoria com `actor_type` user|service|system
- índices multiempresa
- RLS preparada (`qmind_app` + `app.organization_id`)
- seeds de catálogo separados
- testes de isolamento entre duas organizações

## 2. Arquivos

| Arquivo | Papel |
|---|---|
| `../../backend/sql/0001_initial_schema_domain_docs_v0.sql` | DDL canônico (comentado com tag freeze) |
| `../../backend/alembic/versions/20260803_0001_initial_schema_domain_docs_v0.py` | Migração inicial Alembic |
| `../../backend/seeds/*.sql` | Catálogos globais (não migração) |
| `../../backend/tests/test_tenant_isolation.py` | Isolamento A/B |

## 3. Como aplicar (local)

Pré-requisito: container `leaction_db` com database `qmind` (porta host **5433** — emenda #2).

```powershell
cd C:\Projetos\qmind\backend
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
$env:DATABASE_URL = "postgresql+psycopg://admin:password123@localhost:5433/qmind"
alembic upgrade head
Get-Content seeds\001_maturity_catalog_v0.sql -Raw | docker exec -i leaction_db psql -U admin -d qmind
Get-Content seeds\002_assessment_model_stub.sql -Raw | docker exec -i leaction_db psql -U admin -d qmind
pytest -q
```

## 4. RLS

1. `SET LOCAL app.organization_id = '<uuid>';`
2. Conectar como `qmind_app` (não superuser / não owner das tabelas).
3. Políticas `tenant_isolation` com `FORCE ROW LEVEL SECURITY`.

A autorização de negócio continua na aplicação (ADR-002 / ADR-006).

## 5. Fora do v0

- Particionamento / RLS por unidade
- Seed de conteúdo normativo completo (licença)
- Modelos SQLAlchemy ORM completos (opcional futuro)
