import { AssessmentOverviewPage } from "@/pages/AssessmentOverviewPage";

/** Entrada de /assessments/:id — sempre a visão orientada da jornada. */
export function AssessmentEntryPage() {
  return <AssessmentOverviewPage />;
}
