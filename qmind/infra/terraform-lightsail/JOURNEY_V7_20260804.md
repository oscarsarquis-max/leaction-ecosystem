# Gate 011 — V7 Jornada funcional completa (homolog)

- **Resultado:** **PASS**
- **Quando:** `2026-08-04T13:44Z` (UTC)
- **API:** `https://api.homolog.qmind.com.br`
- **Git (workspace):** `d61e463`
- **Imagem:** `qmind-api:mvp-fullstack-v0`
- **Evidência JSON:** `JOURNEY_V7_evidence.json` (sem senhas/tokens/URLs assinadas)

## Escopo executado

Duas organizações Cognito (Org A = jornada; Org B = controle de isolamento).

| Etapa | Resultado |
|---|---|
| Criar/planejar avaliação + iniciar campo | PASS (`draft` → `planned` → `in_progress`) |
| Entrevista + resposta | PASS |
| Evidência S3 authorize→PUT→receive→security_pass + link | PASS (receive idempotente → 409) |
| Constatação create/submit/approve com SoD | PASS (autor 403 `sod_violation`; QM 200) |
| Maturidade fill/submit/approve com SoD | PASS (18 critérios) |
| Plano → item corretivo → start → implemented → validate → confirm_efficacy | PASS (owner validate 403; QM validate→`validated` + efficacy→`done`) |
| Snapshot relatório → submit → publish (aprovador distinto) | PASS (publish idempotente) |
| Export PDF | PASS como **queued** (`job_type=report_pdf_export`; retry idempotente) |
| Close + reopen com motivo | PASS (`closed` → `report`) |
| Org B sem acesso a recursos A | PASS (7/7 → 403/404; sem leak) |
| Auditoria / correlação / sem tokens em metadata | PASS (`secret_hits=0`, `events=53`, `corr=53`) |

## PDF / worker (pendência formal)

| Campo | Valor |
|---|---|
| Job | `queued` (não processado) |
| Worker atual | Compose `sleep infinity` (placeholder) |
| Impacto na V7 | **Não reprova** a jornada — enqueue + idempotência OK |
| Status no gate | **PENDENTE formal** — substituir worker real **ou** aprovar dispensa |
| Dispensa | Só se piloto não depender de PDF, com **prazo**, **responsável** e **critério de encerramento** explícitos |

## Script

`qmind/infra/scripts/journey-e2e-homolog.ps1`

## Próximos (pós-V7)

1. Worker PDF real **ou** dispensa formal documentada no gate  
2. Observação operacional 7 dias (custo/CPU/disco/backup/alarmes)
