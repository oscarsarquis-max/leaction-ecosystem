# Cognito E2E V4 — evidência (gate 011)

| Campo | Valor |
|---|---|
| Resultado | **PASS** |
| Horário (UTC) | `2026-08-04T13:25:20Z` (fecho UI) / API run ~`13:23Z` |
| Git (workspace) | `d61e463` (`mvp-fullstack-v0-4-gd61e463`) |
| Imagens | `qmind-api:mvp-fullstack-v0` / `qmind-web:mvp-fullstack-v0` |
| Pool | `us-east-2_ewD6ck5PM` |
| Client | `306r2id1f5gm9vk733v3rlbda6` |
| Ambiente API | `ENVIRONMENT=homolog` `AUTH_MODE=cognito` |
| SSH ao final | **fechado** (80/443) |

JSON detalhado (sem secrets/tokens): `COGNITO_E2E_V4_evidence.json`.

## Critérios

| Critério | Resultado |
|---|---|
| Usuário homolog exclusivo; senha/token fora de git/docs/logs | **PASS** (efêmeros; só e-mail/`sub`/org ids na evidência) |
| Login UI (Playwright → Cognito hosted → callback `/assessments`) | **PASS** |
| Login API (Bearer + create org) | **PASS** |
| Access token claims (`sub`, `iss`, `client_id`, `token_use=access`) | **PASS** |
| Renovação (REFRESH_TOKEN_AUTH) | **PASS** |
| Logout (GlobalSignOut → refresh revogado; UI limpa storage) | **PASS** |
| Token ausente / adulterado / issuer inválido / assinatura inválida → 401 | **PASS** |
| `AUTH_MODE=dev` / headers `X-Dev-*` rejeitados em homolog | **PASS** (+ settings forbid `AUTH_MODE=dev` com `ENVIRONMENT=homolog`) |
| Org via membership; `X-Organization-Id` sozinho → 401 | **PASS** |
| Cross-org → 403 sem revelar nome da org | **PASS** |
| Secrets/tokens mascarados na evidência | **PASS** |

## Identificadores não sensíveis (amostra da corrida API)

- `user_a_sub`: `61ab25e0-60e1-70ab-812a-f37985f9d4e3`
- `org_a_id`: `8c0c68c8-0d94-4284-8568-e92dfe665d92`
- `org_b_id`: `ce24cda7-12a6-46e6-8245-8006fa3795ff`

## Fixes implantados nesta janela

- JWT Cognito: valida `client_id` (access token) além de `aud`.
- `ENVIRONMENT=homolog` proíbe `AUTH_MODE=dev` (backend + web).
- Cognito client: `ALLOW_ADMIN_USER_PASSWORD_AUTH` para E2E controlado.
- Script: `infra/scripts/cognito-e2e-homolog.ps1` + smoke UI `web/scripts/cognito-ui-login-smoke.cjs`.
