# Variáveis de ambiente do frontend para o build de PRODUÇÃO.
# O Vite carrega este arquivo automaticamente em `npm run build`
# (modo production) e injeta os valores em import.meta.env.
VITE_API_URL=https://metodologiasinovativas.com.br
