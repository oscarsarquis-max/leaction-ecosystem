# CHECKPOINT-GIT-004 — Retorno

Checkpoint operacional de Git do CURSOR-017. Sem Receitas, CURSOR-018, deploy, PR, tag, release ou push forçado.

## Branch e remoto

- Branch: `main`
- Upstream: `origin/main`
- Remoto: `https://github.com/oscarsarquis-max/leaction-ecosystem.git`
- Base anterior: `c64737f9e7cc12a598b8cddfdd8d12a77ee9208b`
- Após `git fetch`, antes do commit: `0` ahead / `0` behind; nenhum commit só local

## Primeiro commit

- Hash: `fdbdceeae3c5ea5406002d11894322c1a997c49b`
- Mensagem: `feat(panne): add ingredient management`
- Push: `c64737f..fdbdcee  HEAD -> main`, sem força

## Escopo incluído

Somente `panne/`: shell Oficina + Atelier, Componentes → Ingredientes (API, migração `0014`, testes, UI), documentação do CURSOR-017, evidências, derivados autorizados dos logos, prompt deste checkpoint e índice.

## Python 3.12

Container oficial `python:3.12-slim-bookworm`, runtime **3.12.14**. Isolado, sem instalação global, sem montar `panne/.env`. URL temporária para `host.docker.internal:5434/panne`; arquivo de ambiente apagado depois.

- Suíte: **204 passed, 1 skipped**
- Skip nominal: `tests/test_ai_bedrock_live.py` — Bedrock vivo desabilitado (`BEDROCK_LIVE_TEST != 1`). Não chama AWS.
- O skip local `tests/test_runtime_url.py::test_unconfigured_runtime_session_is_unavailable` **não ocorreu** no container: a sessão de runtime não está configurada nesse ambiente, então o teste rodou. Não há skip novo nem skip que oculte fallback administrativo.
- Ruff check e format check nos arquivos do CURSOR-017: aprovados
- `ruff check app tests alembic` no tree completo ainda aponta E501/I001 já presentes em arquivos versionados dos ciclos 006–016 (`0006_knowledge_grounding.py`, `production_http`, testes de produção). Não introduzidos por 017; não reformatados.
- `pip-audit`: nenhuma vulnerabilidade conhecida. Pacote local `panne 0.1.0` ignorado por não existir no PyPI.
- `GET /health`: **200** `{"status":"ok","service":"panne","versao":"0.1.0","ambiente":"test"}`
- `GET /ready`: **200** `{"status":"ok","service":"panne"}`

## Migração `0014`

Prova no PostgreSQL local `panne` (Alembic com conexão administrativa):

| Passo | Resultado |
|---|---|
| Head inicial | `0014_ingredient_http` |
| `0014 → 0013` | `ingredient_command` ausente |
| `0013 → 0014` | tabela recriada; `row_version` presente |
| `0014 → 0013` de novo | tabela ausente |
| `0013 → 0014` de novo | tabela presente |
| `0001 → head` | recriação limpa |
| `alembic current` | `0014_ingredient_http` |
| RLS | `relrowsecurity=true`, `relforcerowsecurity=true` |
| Política | `rls_ingredient_command_org` |
| Grants `panne_runtime` | SELECT, INSERT, UPDATE, DELETE |
| Permissões | `ingredient.read\|create\|update_draft\|publish\|retire`, `supplier.read\|manage\|price.record` |
| Triggers | `ingredient_version_nutrition_mass`, `ingredient_version_published_frozen` |

`alembic check` reportou `AutogenerateDiffsDetected`: UniqueConstraint no banco versus índice único nos modelos, em tabelas de todo o schema (incluindo anteriores à `0014`). Não há operação pendente da `0014`. Reversibilidade comprovada.

## Runtime, RLS e autorização

Validado com papel `panne_runtime` (`NOSUPERUSER`, sem `BYPASSRLS`) e suíte HTTP:

- default deny sem contexto (`test_missing_and_invalid_context_deny`)
- organização A não lê nem altera B (`test_isolation_and_org_id_immutable`, `test_permissions_and_rls`)
- `organization_id` imutável
- leitura, criação, rascunho, publicação e aposentadoria
- fornecedor, item e valor de compra append-only
- padeiro sem administração (só `ingredient.read`; POST 403)
- viewer sem mutação (mesmas permissões de ingrediente que o padeiro; matriz explícita)
- sem fallback administrativo
- idempotência da mesma chave
- concorrência HTTP 409 (`If-Match` / `row_version`)

## Frontend e cobertura

Typecheck, lint, **38 testes**, build de produção. Axe existente sem violações críticas.

| Tema | Testes |
|---|---|
| Shell e marca | `ingredients.test` logo/horizontal; `App.test` shell |
| Produção preservada | `App.test`, `ops.test`; quadro permanece |
| Menu Componentes | link presente; oculto sem permissão |
| Lista, filtros e paginação | lista + pesquisa; offset/limit no backend; botões Anterior/Seguinte na página |
| Rascunho e publicado | criar rascunho; versão publicada imutável |
| Composição | editor com constituinte |
| Nutrição e LQ | “abaixo do LQ” |
| Alergênicos | secção no editor |
| Fontes | “Fontes e evidências” |
| Fornecedores e valores | editor + página; append-only; sem a palavra “preço” |
| Assistente | diálogo na edição; lista com axe |
| Badges | “publicado” / rascunho / aposentado |
| Conflito | HTTP 409 |
| Organização e permissões | troca de org limpa recorte; menu some sem permissão |
| Acessibilidade e responsividade | axe na lista; `prefers-reduced-motion`; evidências desktop/notebook/tablet |

Sem lacuna crítica de cobertura.

## Head do Alembic

`0014_ingredient_http` (head)

## Segurança do staging

- `git diff --cached --check` limpo
- Sem `.env`, token real, chave, senha ou URL autenticada
- Sem `node_modules`, `dist`, caches, `.venv`
- Logos mestres intactos: `pannebege.png` `52a42483…`, `pannepreto.png` `2962bb9d…`
- Nenhuma aplicação irmã
- Nenhum trabalho do CURSOR-018

## Arquivos excluídos

- `panne/.env`
- `panne/.tmp-chrome-017/`
- `node_modules`, `dist`, caches, `.venv`
- `apps/diario-obra-api/diario-start.err`
- `infra/deps-install-log.txt`
- `leaction-platform/services/gateway-api/mp-log-20260719-1015.txt`
- `phanton/database/_lan-sync/`

## Estado do Git após o primeiro push

`main` alinhada a `origin/main` em `fdbdcee`. Permaneceram não rastreados os leftovers acima.

## Deploy, PR, tag e release

Não houve deploy, PR, tag, release, pull, merge, rebase nem push forçado.

## CURSOR-018

**Não iniciado.**
