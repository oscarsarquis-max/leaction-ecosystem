# Segurança e RLS de relatórios

ENABLE+FORCE, default deny por `organization_id = panne_current_org_id()`. Runtime sem fallback administrativo. Cache de consulta segmentado por usuário, organização, permissões e filtros. Logs sem token.
