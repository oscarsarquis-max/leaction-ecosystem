# CSV e impressão

CSV UTF-8 com metadados (`#`) e colunas estáveis. Texto iniciado por `= + - @` recebe prefixo `'`. Valor numérico permanece número. Ausente vai vazio; zero conhecido vai `0`. Impressão HTML/A4 sem menus. Sem XLSX, PDF certificado, e-mail ou agendamento.
