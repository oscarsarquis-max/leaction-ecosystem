# Concorrência e idempotência

`Idempotency-Key` em comandos. `If-Match`/`row_version` em estados mutáveis. Lock em saldo, reserva, recebimento e inventário. Duas saídas concorrentes: uma falha com `saldo_insuficiente`.
