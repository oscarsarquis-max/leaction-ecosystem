# Comandos e feedback

`useCommand` gera uma chave por intenção. Retry do mesmo conteúdo reutiliza a chave. Conteúdo diferente gera chave nova. Duplo clique é bloqueado. HTTP 409 pede recarregamento. Erros ficam no painel, não só em toast. Não há sucesso otimista em ações irreversíveis.
