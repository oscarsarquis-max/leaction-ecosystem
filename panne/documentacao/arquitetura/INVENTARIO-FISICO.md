# Inventário físico

Sessão por local e data de corte. Escopo congelado. Contagens append-only (1ª e 2ª). Divergência calculada. Aprovação gera ajustes. Fechamento imutável. Reabertura proibida.

Durante a sessão o local pode ficar bloqueado (`lock_location_on_count`).
