# Proposta CURSOR-012 (não executar)

Fatia documental e, se o arquiteto aceitar, **só** o desenho fechado do DDL — ainda sem migrar se a descoberta com usuários estiver pendente.

Escopo sugerido:

1. fechar as questões de prioridade 1 com o proprietário;
2. especificar a primeira migração (`ProductionPlan`, `ProductionOrder`, `ProductionBatch`, snapshots, eventos, emissão) **em documento**, alinhada a 0009;
3. não abrir quadro, ficha PDF nem CRUD HTTP nesse ciclo, salvo decisão explícita de “mínimo `GET` de quadro”;
4. não implementar custos;
5. não tratar `trial` como ordem;
6. preservar CURSOR-001–011.

Fora: estoque, marketplace, frontend completo, IA operando estados.

Este arquivo **não** é autorização de execução.
