# Instruções do laboratório

Abra `panne/design/ux-001/index.html` no navegador, sem Vite e sem rede. Detalhes em `panne/design/ux-001/ABRIR.md`.

A **Direção aprovada — Oficina + Atelier** é o padrão (`dir=aprovada`). Atelier, Oficina e Mesa permanecem no seletor como histórico.

Parâmetros: `dir`, `tela`, `papel`, `menu`, `assistente`, `guia`, `estado`, `densidade`.
