'use client';

import { InoveEcosystemHero } from '@/components/public-portal/InoveEcosystemHero';

/** Coluna central — identidade /comeco → CTA /comeco (código, não CMS). */
export function PortalHeroColumn() {
  return <InoveEcosystemHero />;
}
