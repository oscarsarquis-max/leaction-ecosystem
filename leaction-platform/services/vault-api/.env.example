# Cofre — NÃO reaproveitar DATABASE_URL / JWT_SECRET do Action Hub
VAULT_DATABASE_URL=postgresql://vault_api:vault_local_change_me@127.0.0.1:5434/leaction_vault
VAULT_MASTER_KEY=
VAULT_JWT_SECRET=
VAULT_PORT=4020

# Só o script apply-schema.js (cria o banco/role). O processo vault-api não usa isto.
VAULT_BOOTSTRAP_DATABASE_URL=postgresql://admin:password123@127.0.0.1:5434/leaction_hub
VAULT_DB_PASSWORD=vault_local_change_me

# seed-admin.js
VAULT_BOOTSTRAP_EMAIL=admin@cofre.local
VAULT_BOOTSTRAP_PASSWORD=
