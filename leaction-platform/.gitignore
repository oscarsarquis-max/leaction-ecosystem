# Ambiente local / produção (nunca commitar segredos)
.env
.env.local
.env.production
.env.*.local

# Release SHA gerado no deploy (não versionar)
GIT_SHA

# PM2
logs/
.pm2/

# Dev orchestration (scripts/dev/start-hub.ps1)
.dev-logs/

# Node
node_modules/
frontend/action-hub/.next/
services/gateway-api/node_modules/
services/gateway-api/cms-uploads/

# Chaves SSH / PEM
chaves/
*.pem

# OAuth Mercado Livre (Marketplace plugin)
backend/.ml_tokens.json
backend/.env.ml.secrets

.deploy-secrets/

